# v4.60 — CN data-transfer artwork refresh

- Uses the operator-supplied final RGBA `npps4_data_transfer.png` without regenerating or redrawing it.
- Rebuilds the working CN WebView texture pair `wv_ba_117.png.imag` + `tx_wv_ba_117.texb` from that exact PNG.
- Keeps the proven `4_0_999.zip` normal full-data delivery path and the `assets/image/webview/wv_ba_117.png` banner contract.
- No banner routing, transfer workflow, manga flip, scouting, Museum, accessory, economy, or download-version logic is changed.
- Android and PC Python server trees remain byte-identical.
